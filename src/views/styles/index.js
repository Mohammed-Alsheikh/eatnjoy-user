import {Dimensions} from 'react-native';

const Colors = {
    background: "#E1E1E1",
    primary: '#690D12',
    secondary: '#0A475C',
};

const {width, height} = Dimensions.get('window');

export {width, height};
export default Colors;